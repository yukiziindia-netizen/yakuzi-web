const z = require('zod'); const schema = z.object({ price: z.preprocess(v => Number(v) || 0, z.number()), variants: z.array(z.any()).optional() }).superRefine((data, ctx) => { if (!data.variants?.length && data.price < 0.01) ctx.addIssue({ code: z.ZodIssueCode.custom, message: 'Price required', path: ['price'] }); }); console.log('Empty price with variants:', schema.safeParse({ price: NaN, variants: [{}] }).success); console.log('Empty price no variants:', schema.safeParse({ price: NaN }).success);
